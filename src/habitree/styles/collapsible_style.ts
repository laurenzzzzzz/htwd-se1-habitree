import { StyleSheet } from 'react-native';

export const collapsibleStyles = StyleSheet.create({
  heading: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
  },
  content: {
    marginTop: 6,
    marginLeft: 24,
  },
  iconStyle: {
    width: 24,
    height: 24,
  },
});
